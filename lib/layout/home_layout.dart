import 'package:flutter/cupertino.dart';

class HomeLayout extends StatelessWidget {
  @override
  static const String routeName = "home layout";

  Widget build(BuildContext context) {
    return Center(child: Text("Home Layout"));
  }
}
